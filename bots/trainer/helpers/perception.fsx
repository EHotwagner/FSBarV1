// bots/trainer/helpers/perception.fsx — stub for Trainer perception helpers
//
// Intentionally empty. Helpers land here only by extraction from duplicated logic across
// two or more consecutive bot.fsx revisions (FR-020). Do not pre-emptively add members.

// log.fsx must be #loaded before this file. bot.fsx does that in order.
let trainerPerceptionVersion = "0.0-empty"
printfn "[trainer] perception.fsx loaded"
