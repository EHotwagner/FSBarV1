module FSBar.Hub.LiveTests.PlaceholderTests

// Phase 1 skeleton. Real live tests — LiveSessionLaunchTests (US1),
// LiveProxyInstallTests (US2), LiveGrpcStreamTests (US7) — land in their
// respective user-story phases.

open Xunit

[<Fact>]
let ``FSBar.Hub.LiveTests skeleton is wired into the test host`` () =
    Assert.True(true)
