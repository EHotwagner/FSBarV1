module FSBar.Hub.Tests.PlaceholderTests

// Phase 1 skeleton. Real tests (HubSettingsTests, BarInstallTests, etc.)
// land in Phase 2 (tasks T020–T022a). xUnit discovery requires at least one
// compiled test class so the skeleton project can be test-discovered.

open Xunit

[<Fact>]
let ``FSBar.Hub skeleton is wired into the test host`` () =
    Assert.True(true)
